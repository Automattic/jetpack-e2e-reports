name: Dashboard data

on:
  workflow_run:
    workflows: [ "Report", "Cleanup" ]
    branches:
      - trunk
    types:
      - completed
  workflow_dispatch:

concurrency:
  group: ci-${{ github.head_ref }}
  cancel-in-progress: true

jobs:
  dashboard:
    name: "Create summary data"
    runs-on: ubuntu-latest

    steps:
      - name: Setup Node
        uses: actions/setup-node@v2

      - name: Checkout
        uses: actions/checkout@v2

      - uses: actions/cache@v2
        with:
          path: ~/.npm
          key: ${{ runner.os }}-node-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-node-

      - name: Recreate summary file
        run: node ./bin/create-summary.js

      - name: Recreate metrics file
        run: node ./bin/capture-metrics.js

      - name: Push changes
        env:
          MESSAGE: "Automatic summary data update"
        run: ./bin/push-changes.sh
